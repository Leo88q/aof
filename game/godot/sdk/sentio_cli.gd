class_name SdkSentioCli
extends RefCounted
## v3 SDK: sentio-cli — tx tracing + anomaly pipelines.
func config() -> Dictionary: return {"sdk": "sentio-cli", "checks": ["tx tracing", "entity flows", "alert rules"]}
