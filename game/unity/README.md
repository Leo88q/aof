# AOF — Unity mirror (v1 7 layers + v2 12 products + v3 13 best-free SDKs)

C# mirror of `game/godot/` for **Age of Farming** (game_id `aof`, tenant `aof`,
network `stage` / `prototype`). Feature parity doc: `game/README.md`.
Config source of truth: Watchtower OS v3 `GET /api/os/config` + `GET /api/sdk/<id>?gameId=aof`.

- `Assets/Scripts/Layers/`   — v1: L1 Platform … L7 Ops
- `Assets/Scripts/Products/` — v2: 12 products (farming … cross-game materials)
- `Assets/Scripts/Sdk/`      — v3: 13 best-free SDKs (gamba … arcium)
