# Непринятые findings внешнего baseline

Это review, не новый внешний аудит. Ни один finding не помечен accepted-risk; владелец должен быть назначен командой. Все 17 остаются открытыми до независимой проверки.

| Rule | Severity | Текущий файл:строка | Причина / изменение | Экономическое последствие |
|---|---|---|---|---|
| SW016 | high | `programs/aof-rebirth/src/instructions/do_rebirth.rs:8` | Fail-closed FeatureDisabled до изменения экономики; init_if_needed сохранён для повторного rebirth. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Без атомарного сброса прогресса включение создаст бонус без положенного sink. |
| SW008 | high | `programs/aof-market/src/lib.rs:409` | Reload обоих SPL token accounts после CPI; order не reload, иначе теряется active=false. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Stale balance или повторная отмена могут искажать escrow/refund. |
| SW027 | low | `programs/aof-market/src/lib.rs:291` | GlobalFeesUpdated уже был в исходном checkout; защита от удаления emit в CI. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Неучтённая смена fees искажает доходность и сверку treasury. |
| SW027 | low | `programs/aof-market/src/lib.rs:298` | GlobalPausedUpdated уже был в исходном checkout. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Невидимый pause затрудняет остановку потерь. |
| SW027 | low | `programs/aof-market/src/lib.rs:409` | LimitOrderCancelled уже был в исходном checkout. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Без события отмены ledger сохраняет фиктивную liability. |
| SW016 | high | `programs/aof-liquidity/src/instructions/lp_deposit.rs:26` | init_if_needed нужен для повторного депозита; checked_add сохраняет counters; close/assign/drain путей нет. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Сброс shares/reserve способен обесценить LP-позиции. |
| SW016 | high | `programs/aof-liquidity/src/instructions/lp_deposit.rs:37` | init_if_needed нужен для повторного депозита; shares прибавляются, deposited_at заполняется лишь один раз; проверены user/rarity. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Сброс или подмена позиции нарушает право на резерв. |
| SW024 | high | `programs/aof-liquidity/src/state/lp_pool.rs:31` | checked_div уже присутствовал; добавлено 10000 детерминированных full-u64 property случаев. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Нулевые assets либо overflow не должны создавать shares или блокировать процесс panic. |
| SW024 | high | `programs/aof-liquidity/src/state/lp_pool.rs:45` | checked_div и shares<=total уже присутствовали; дополнены экстремальные случаи. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Вывод не должен превысить principal/fees резервы. |
| SW001 | critical | `programs/aof-session-keys/src/lib.rs:190` | authority теперь Signer; добавлен негативный Anchor try_accounts тест; spending по-прежнему отключён. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Без авторизации можно резервировать чужой spending budget; атомарная привязка CPI всё ещё отсутствует. |
| SW016 | high | `programs/aof-session-keys/src/lib.rs:136` | Oracle signer/has_one, system owner субъекта, identity constraint, monotonic epoch; overwrite score является авторизованным обновлением, не reset budget. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Компрометация oracle меняет trust tier и будущие лимиты; signer владельца не заменяет oracle governance. |
| SW016 | high | `programs/aof-session-keys/src/lib.rs:154` | init вместо init_if_needed; исправлен SESSION_SPACE с 130 на 146; создание остаётся disabled. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Повторное создание не должно сбрасывать spent_today/revocation. |
| SW013 | high | `programs/aof-session-keys/src/lib.rs:136` | Владелец user проверяется через owner=System; stored trust.user связан с seed; oracle подписывает обновление. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Чужой snapshot не должен увеличивать лимит игрока. |
| SW013 | high | `programs/aof-session-keys/src/lib.rs:190` | Signer authority + seeds + stored authority/session_signer constraints; негативные тесты подмены. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Подмена namespace может использовать чужой лимит. |
| SW027 | low | `programs/aof-session-keys/src/lib.rs:270` | SessionRevokedEvent уже присутствовал; source regression запрещает удаление. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Необнаруженная ревокация позволяет off-chain ботам продолжать попытки расхода. |
| SW027 | low | `programs/aof-session-keys/src/lib.rs:280` | SessionPausedEvent уже присутствовал. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Невидимый pause мешает security monitoring и остановке бота. |
| SW013 | high | `programs/aof-quests/src/instructions/drum/drum_reveal.rs:26` | user явно address=drum_commit.user; seeds, stored relation, authority signer и close сохраняются. Не принят: нет SVM transaction evidence + независимого rescan (host Rust CI прошёл). | Подмена получателя или повторный reveal выводят prize treasury. |
